# External skill / agent references that intentionally appear in SKILL.md routing
# tables without a namespace prefix. The audit script (audit-agent-refs.ps1) loads
# this file and treats matches as resolved (not orphans).
#
# Format: one identifier per line. Blank lines and lines starting with '#' are ignored.

# === Skills the user has installed under other plugins ===
internal-comms
internal-comms-20260419
doc-coauthoring
compose-outreach
draft-outreach
call-prep
weekly-prep-brief

# === ECC build-error-resolver skills (cluster reference) ===
flutter-build
go-build
gradle-build
kotlin-build
rust-build

# === Hypothetical / planned exam engines (referenced as "if you have X, use it") ===
korean-legal-engine
us-filing-engine
uscpa-bar-engine
professional-exam-mastery

# === ECC agent referenced cross-namespace ===
security-reviewer

# === Built-in Claude Code slash commands referenced as skills ===
review
security-review
